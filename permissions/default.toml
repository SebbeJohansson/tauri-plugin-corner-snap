"$schema" = "schemas/schema.json"

[default]
description = """
Lets the webview move the window between its configured states, and ask for
it to be re-parked against its anchor.
"""
permissions = ["allow-set-state", "allow-snap"]
